name: Build and Release

on:
  push:
    branches: [ main, develop ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main, develop ]

env:
  DOTNET_VERSION: '6.0.x'
  CONFIGURATION: Release
  ARTIFACT_NAME: Jellyfin.Xtream.V3

jobs:
  build:
    name: Build Project
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --configuration ${{ env.CONFIGURATION }} --no-restore

    - name: Run benchmarks
      run: dotnet build --configuration ${{ env.CONFIGURATION }} -p:BenchmarksEnabled=true
      continue-on-error: true

    - name: Publish artifacts
      run: dotnet publish -c ${{ env.CONFIGURATION }} -o ./publish

    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: ${{ env.ARTIFACT_NAME }}
        path: ./publish
        retention-days: 30

  test:
    name: Test Project
    runs-on: windows-latest
    needs: build

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Restore dependencies
      run: dotnet restore

    - name: Run tests
      run: dotnet test --configuration ${{ env.CONFIGURATION }} --no-restore --verbosity normal
      continue-on-error: true

  release:
    name: Create Release
    runs-on: windows-latest
    needs: [build, test]
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write
      repository-projects: read

    steps:
    - name: Checkout code
      uses: actions/checkout@v3
      with:
        fetch-depth: 0

    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: ${{ env.DOTNET_VERSION }}

    - name: Get tag version
      id: tag
      run: |
        $tagRef = "${{ github.ref }}"
        $version = $tagRef -replace '^refs/tags/', ''
        Write-Host "Extracted version: $version"
        "version=$version" | Out-File -FilePath $env:GITHUB_OUTPUT -Encoding utf8 -Append
      shell: pwsh

    - name: Restore dependencies
      run: dotnet restore

    - name: Build release
      run: dotnet build --configuration ${{ env.CONFIGURATION }} --no-restore

    - name: Publish release
      run: dotnet publish -c ${{ env.CONFIGURATION }} -o ./release

    - name: Create archive
      run: |
        cd ./release
        Compress-Archive -Path * -DestinationPath ../Jellyfin.Xtream.V3-${{ steps.tag.outputs.version }}.zip
        cd ..
      shell: pwsh

    - name: Create GitHub Release
      uses: softprops/action-gh-release@v1
      with:
        files: ./Jellyfin.Xtream.V3-${{ steps.tag.outputs.version }}.zip
        body_path: RELEASE_NOTES.md
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ github.token }}

    - name: Upload to releases
      run: |
        Write-Host "Release ${{ steps.tag.outputs.version }} published successfully!"
      shell: pwsh
